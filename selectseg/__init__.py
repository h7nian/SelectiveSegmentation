"""Loss-indexed confidence for selective binary segmentation.

The focused pipeline lives in :mod:`selectseg.binary_framework` and
:mod:`selectseg.binary_eval`.  The older multiclass/band implementation is
retained separately for provenance and is not imported by the focused API.
"""

__version__ = "0.1.0"
